# path where 'temp' file would be created
PATH_TO_DEVICES = 'devices/temp'
# pin which is virtually connected to the sensor
TEMP_PIN = 5
# global process which would continually log temperature values into the text file in background
PROCESS = None
